# Changelog

## [0.2.0] - 2026-07-07

### Changed
- Allow-list replacers now keep all elements in arrays that the allow list keeps (#16).
- Parse revivers now see resolved shared and cyclic containers before they replace values (#17).

### Fixed
- Deep valid JSON arrays and objects now parse instead of aborting the process (#15).

## [0.2.0] - 2026-07-07

### Changed
- Allow-list replacers now keep all elements in arrays that the allow list keeps (#16).
- Parse revivers now see resolved shared and cyclic containers before they replace values (#17).

### Fixed
- Deep valid JSON arrays and objects now parse instead of aborting the process (#15).
